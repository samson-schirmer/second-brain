# Replit Context — Second Brain

> This file is a generated read-only projection. Do not edit by hand. Source of truth: `schema/WIKI_SCHEMA.md`.

---

## What This Repo Is

Samson Schirmer's personal second brain — a git-tracked, LLM-maintained knowledge base that serves as the single source of truth for his work context across all AI platforms.

## How to Read It

1. Start with `index.md` — the catalog of all wiki pages.
2. Drill into `wiki/` for synthesized knowledge (people, initiatives, concepts).
3. Check `MEMORY_CONTEXT.json` for structured state (active workstreams, team, decisions).
4. Reference `Personal_Context/` for detailed profile pages (identity, role, projects, team, communication style).

## Key Pointers

- **Operating contract:** `schema/WIKI_SCHEMA.md` — defines how the wiki works.
- **Structured state:** `MEMORY_CONTEXT.json` — workstreams, team roster, decision log.
- **Navigation:** `index.md` — categorized page catalog.
- **Activity log:** `log.md` — chronological record of all operations.

## Do Not

- Write to `wiki/` (single-writer rule — the librarian agent maintains it).
- Treat `raw/` content as editable.
- Fabricate facts — if unknown, say so.
